# sql_bestfmt

Synthetic data. Regression guard for BUG-sqlbestfmt:
fractional PROC SQL aggregate results render via BEST12. (format.bestNum), matching
the DATA step, rather than Zig's raw {d}. **Passes**.
