# proc_datasets

Synthetic data. Fixture for CLIN-datasets: PROC
DATASETS APPEND / MODIFY(RENAME,LABEL,FORMAT) / DELETE / CHANGE. The final member
(base appended with extra, x renamed to score, member renamed to combined) is
written out and diffed. **Passes**.
