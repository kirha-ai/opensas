# OpenSAS — open backlog

Known gaps and bugs, filed against the SAS 9.4 documentation. Every item is
either fail-loud today (exit code 2 or 1) or a documented divergence; nothing
here is a silent wrong result that we know of.

House rules for anyone picking one up: `CLAUDE.md`. Settled design decisions:
`docs/decisions.md`. Every fix ships a `test` block or a `tests/corpus/*.sas`
fixture with its expected `.txt`.

State: `TODO` assignable · `BLOCKED` deps unmet · `PARK` known, deferred.
Line format: `- [STATE] ID — title  [owns: path]  [deps: …]`.
`needs-oracle` = the in-repo docs do not settle it; a run against real SAS 9.4 does.

---

- [TODO] PERF-inlistchar (perf audit) — character `IN (...)` lists stay OR-chains: `if u in (<1000 char literals>)` 1M rows = 57.95s (K=100: 5.54s) vs numeric-IN set path 0.46s (>126×); SQL WHERE identical (56.94s). Hotspot: evalBinary OR-chain spine per row; buildInSet eval.zig:238 is ArrayList(f64) — numeric-only by construction. Fix: extend InSet recognition to all-char-literal lists (StringHashMap on the lhs var's TRIMMED value; SAS trailing-blank comparison semantics = the correctness trap). Target: K=1000 × 1M ≤ ~1.5s (setcopy floor 0.86s); DONE gate = measurement re-run + byte-identical suite output. (HANDOFF STOP: a second agent was ALSO killed mid-task on this — its uncommitted WIP in worktree agent-a3bbac6's eval.zig has a KNOWN BUG the agent self-reported and had not fixed: "mixed-type guard has a first-element bug — char_mode starts false, so a leading char list would bail". Fix the order or start fresh.)  [owns: src/eval.zig]
- [TODO] NOTE-perfcasestack (perf pass-4 PRELIMINARY signal — killed before clean re-measurement; CPU contention admitted, sampling not done, NOT ticket-grade yet) — CASE-heavy select over 1M rows measured 237s/1.65GB and ORDER-BY-computed with outobs=10 measured 14.8s/1.18GB (SQL advanced surfaces, pass-4). Both need clean re-runs (serial, sampled) before promotion to PERF tickets — if they hold, they are likely superlinear (outobs=10 still costing seconds suggests post-projection waste).  [owns: src/sql.zig] — per-call prxparse compiles + retains ~0.6KB for the whole run: naive `if prxmatch(prxparse("..."), u)` per row, 1M rows = 2.31s / ~1.23GB peak RSS (~+580B per call over the 788MB floor); parse-once control (`retain re; if _n_=1 then re=prxparse(...)`) = 0.04s / 237MB — the entire cost is recompilation, not matching. Site: functions.zig:474 prxparse → prx.parse; module-global registry prx.zig:631/669 lives to end of run. Fix: intern identical pattern strings (pattern → existing id). Target: inline-idiom 1M ≤ ~0.5s, RSS within ~10% of parse-once control; DONE gate = measurement re-run + byte-identical suite output.  [owns: src/functions.zig — prx.zig may be touched too if interning lives there (both free)] (HANDOFF STOP: previous agent stopped before committing.)
- [TODO] GAP-sortseq-procopt-verify (dev4 sortseq tick residual b, LOW) — data sorted with PROC-level `sortseq=linguistic` (PROC option, system option unset) is still BY-verified byte-wise; honoring it needs per-dataset collation state, not just the global flag.  [owns: src/proc.zig]
- [PARK] PERF-iomultiplex (perf audit pass-3 observations — NO timeout tripped, promote only if one ever does) — (a) dir-libname write materializes dataset + whole sas7bdat buffer + whole CSV sidecar simultaneously (main.zig:1028 writeMember, two allocPrint+writeFile): 1.6M×10 write peaks 3.2GB for a 467MB dataset; streaming the sidecar ≈ halves write wall, −40% peak RSS. (b) in-memory Dataset rep ≈ 4–5× raw file size (1M-row read = 1.38GB RSS for a 292MB file). Hotspot if revisited: io.writeCsvImpl per-cell bestNumW→allocPrint ≈46% of write wall (io.zig:1371). Probes: /tmp/opensas-perf/io.  [owns: src/main.zig, src/io.zig]
- [TODO] GAP-macrooptfamily (doc-finder, MED run-killer + rc misclass) — documented macro system options (Macro Ref ch.20, all "Valid in: ... OPTIONS statement") exit rc 1 "not recognized" AND errhalt-skip later steps: mautosource / sasautos=(dir) (sharpest — opensas HAS an autocall engine macro.zig:486 but only --sasautos/$SASAUTOS reach it; an OPTIONS-set SASAUTOS must resolve macros), nomreplace (also silently ALLOWS redefinition before erroring), mcompilenote=, mfile+mprint fileref; documented-but-absent family: MACRO, MCOMPILE, MCOVERAGE, MCOVERAGELOC=, MEXECNOTE, MEXECSIZE=, MLOGICNEST, MPRINTNEST, MSYMTABMAX=, MVARSIZE=, CMDMAC, IMPLMAC, MAUTOCOMPLOC family. Per §5d/D-018: honour, or exit rc 2 as gap — never rc 1 typo.  [owns: src/main.zig, src/macro.zig] (HANDOFF STOP: previous agent killed mid-edit, no commit.)
- [TODO] GAP-mprint-loglines (doc-finder, MED log-parity) — MPRINT/SYMBOLGEN/MLOGIC are inert: rc 0, zero log lines, but the doc says MPRINT writes macro-generated statements to the log (lrcon p.207; Macro Ref pp.165-166 example `MPRINT(OUTER):   data _null_;`; SYMBOLGEN p.485; nesting pp.475-476). This project treats log content as observable (nonotes honoured). Minimum per D-022(b): a NOTE; conformant: the lines. Owner: macro.zig expansion sites + main.zig isInertOption reclass. (HANDOFF STOP: prior agent killed mid-edit — UNCOMMITTED WIP in worktree agent-ac08ce6's macro.zig, last note "Now SYMBOLGEN in resolveAmpRun"; review before reuse.)  [owns: src/macro.zig]
- [TODO] BUG-marginunit-suffix (doc-finder, valid option kills run) — `options leftmargin=0.5in;` → `ERROR: system option in is not recognized` rc 1 (margins ARE system options, lrcon ch.15 p.322; the <n><unit> value form is System Options: Reference syntax — volume not in docs/, and D-018 says absence is not absence). Cause: skipOptionValue main.zig:1789 consumes only 1-char magnitude suffixes (k/m/g) — `0.5` lexes as number, `in` as NAME → unknown-option arm. Same species as the fixed obs=2k half of BUG-optionsstmtswallow.  [owns: src/main.zig]
- [TODO] GAP-opt-unknown-sweep (doc-finder, D-018 sweep) — the OPTIONS chain's final else (main.zig:1584) misclassifies DOCUMENTED options as typos (rc 1 instead of honour/inert/rc 2): errors=1 (lrcon p.207 — documented IN OUR VOLUME), vnferr (p.186), dkqcond= (family of honoured DKRICOND=), validvarname=v6 (documented VALUE rejected — exact D-018 shape), noxwait/noxsync (host options, D-022 pairing → inert-accept); verify-first against System Options: Reference: missing=N unquoted (may be legal).  [owns: src/main.zig]
- [TODO] BUG-8601dparam (doc-finder, MED-HIGH silent precision loss) — write side drops `.d` fractional seconds on all four DT/TM formats: doc p.201 example `e8601dt25.3` → `.000` silently lost (d Range 0–6 documented on E8601DT/TM/B8601DT/B8601TM; informat side fine). Fix: dispatch never passes spec.d (L1813-1816/1826/1836-1837) + renderers lack the d parameter (renderE8601Dt L2895 admits it, renderB8601Dt L2961, renderB8601Tm L2984, renderTime L2739). E8601DZ defines no d — leave alone.  [owns: src/format.zig]
- [DOING @dev3] BUG-8601dzalign — E8601DZ format is RIGHT-JUSTIFIED in real SAS (ORACLE CONFIRMED, user run 0923-v2: `f26=" 2018-09-15T14:52:22+00:00"` n26=26; `f22="  2018-09-15T14:52:22Z"` n22=22 — leading blanks at both rungs); opensas left-justifies while its own MISSING branch already right-justifies (internal inconsistency dies with the fix). Fix: renderE8601Dz value path justLeft→justRight (~L2958); update gfmt_e8601dnz golden to the oracle lines + re-anchor its comment from the non-oracle Alignment header to the probe. Family-wide: D-025 extension — the WHOLE ISO 8601 family right-justifies (oracle), catalog `Alignment: Left` metadata is wrong family-wide. Bundled: BUG-b8601dtdefwidth.  [owns: src/format.zig, docs/decisions.md]
- [DOING @dev3] BUG-b8601dtdefwidth (bundled with dzalign, same theme) — B8601DTw. DEFAULT width is 19 (doc p.152; oracle n7=19: `f7="    20180915T145222"`), opensas renders 15 (renderB8601Dt ~L2962 default + a ponytail contradicting the doc's own number). Fix: default 15→19. EXPECTED golden move: tests/corpus/gfmt_writebatch.txt B8601DT line gains 4 leading blanks (oracle-verified, intentional).  [owns: src/format.zig]
- [TODO] BUG-8601widths (doc-finder, MEDIUM) — width Range/Requirement unenforced on 10 family surfaces: formats e8601da/e8601dn/e8601tm/b8601da/b8601dt/b8601tm + informats e8601da/e8601tm/b8601dt/b8601tm silently accept out-of-range widths (`e8601da5.` drops the century AND overflows the 5-wide field; `b8601dt14.` writes 15 chars; e8601da informat silently reads while twin e8601dn errors). Fix: checkWidthRange (~L1600, incl. the single-width `must be 10` variant) beside L1809/1827/1826/1835-1837 + informat L672/676/655; add b8601dt informat's missing slice-to-w (mirror L650-653). ORACLE-CONFIRMED (user run 0923): real SAS rejects `e8601da12.`/`e8601dn12.` at COMPILE time with `ERROR 29-185: Width specified for format E8601DA is invalid.` and stops the step — premise proven; opensas's render-time checkWidthRange is the accepted equivalent (loud, rc 1).  [owns: src/format.zig]
- [TODO] BUG-8601yearonly (doc-finder, LOW-MED) — B8601DA/B8601DT informat omission rule absent: doc says omitted month/day → 1 (hour/min/sec → 0); year-only `2018` reads MISSING instead of doc-example 21185/1830384000. Fix: parseIsoDate (L1109) short-form ladder zero-defaulting month/day before date math; keep the leniency OFF the E paths (the sentence lives in B entries only — verify if cheap).  [owns: src/format.zig]
- [TODO] GAP-8601b8601dndz (doc-finder, LOW) — B8601DN/B8601DZ unimplemented all four directions (loud rc 2, correct class — just untracked). Whitelist L1521-1526 + read branch L655-680 + write dispatch L1835-1837; parseDatetimeZ (L942) ponytail points at the packed `+hhmm` branch; DZ format ladder mirrors renderE8601Dz; B8601DN = datePart + renderYymmddX at fixed w=10.  [owns: src/format.zig]
- [BUG-CONFIRMED] ORACLE-crossread-half (user run 0923) — real SAS is STRICT per name: `b8601da.` ACCEPTS extended `2018-09-15` → 21442, but `e8601da10.`/`e8601dn10.` REJECT packed `20180915` → missing + invalid-arg NOTE + _ERROR_=1. opensas reads every cross-notation pair = silent-accept divergence. Fix: e8601da/e8601dn read paths reject packed-only digits (missing + NOTE); keep b8601da's observed leniency.  [owns: src/format.zig]
- [TODO] GAP-macrooptfamily (doc-finder, MED run-killer + rc misclass)
---

- [TODO] NOTE-symputxglobalnolatebind (@dev-sy ponytail from GH#77-2, LOW, `ponytail:`-marked at the site) — `symputx(...,'G')` under a LIVE local frame lands in `State.vars` rather than the exec→macro queue, so a step ALREADY LEXED before the `%MEND` cannot late-bind such a global. Ordinary `&name` resolution after the macro returns does see it, and the fixture pins that. Bounded and documented, not silent.  [owns: src/macro.zig]
- [TODO] GAP-obscountnote (manager tick-GH82, LOW log-parity — **filed from a NOT-A-BUG validation**) — real SAS logs `NOTE: The data set WORK.T5 has 1 observations and 1 variables.` for EVERY DATA step; **opensas emits no obs-count NOTE at all, for any step.** Measured, not assumed: `grep -rn '"NOTE' src/*.zig` finds the obs-count strings only inside COMMENTS (`main.zig:911`, `main.zig:4902`, `io.zig:2391`), never at an emission site, and zero corpus goldens contain `observations and`. It is a specific gap rather than "no log" — uninitialized-variable and missing-value NOTEs both fire correctly. Landing this moves a LOT of goldens, so it needs its own quiet slice.  [owns: src/main.zig, src/io.zig]  [deps: main.zig lock]
- [TODO] BUG-badaritywrongtype (**RE-SCOPED tick-GH-intake by @coder-cf, which MEASURED it and then correctly REVERTED rather than sprawling — read this before spending a slot**) — `badArity` (`functions.zig:~3716`) returns `Value.missing` for EVERY function, so a wrong-arity call to a CHARACTER function returns the wrong type. **Reproduces end to end:** `upcase('abc','x')` → `vtype=N`, then `put (a) ($char10.);` → `ERROR: The character format $char10. cannot be used with a numeric value.` rc 1; while `abs(1,2)` → N is CORRECT, so a blanket flip is wrong. **THE GOLDENS ARE NOT THE CONSTRAINT — THE DIFF IS.** Measured with a deliberately maximal probe (blanket `.{ .str = "" }` for every function): **corpus 1866/1866 and programs 11/11 STILL GREEN, blast radius ZERO** — the corpus never exercises `badArity`'s return value, and no golden `.txt` even contains the arity NOTE because it goes to STDERR and only STDOUT is diffed. Only 2 in-file tests moved and BOTH pin numeric functions (`logistic`, `abs`) — i.e. they are the guardrail DISPROVING the blanket fix, not evidence for it. Right shape is `badArityChar` as a sibling of `badArity` exactly as `domErrChar` is of `domErr`. **Cost: 264 call sites** (functions 82, charfns 67, statfns 39, datefns 35, numfns 24, finfns 18); a rough classifier over two files gave ~32 clearly char, ~30 clearly numeric and **~86 AMBIGUOUS** needing a per-function `Categories:` lookup — ~50-90 hand-classified one-liners across ≥3 files, **validated only by ~90 doc lookups because NO golden can confirm any of them**. **DO NOT DISPATCH THIS UNTIL THE ORACLE QUESTION IS SETTLED: real SAS 9.4 appears to reject wrong arity at COMPILE time (`ERROR: Function ABS requires 1 argument…`) and not execute the step — under which the return type is MOOT and this whole class evaporates.** @coder-cf could NOT substantiate that from the shipped volumes (the Functions Reference documents no wrong-arity behaviour anywhere; greps for 'too many arguments' / 'number of arguments' hit only unrelated prose) and correctly did not act on it. **One live-SAS run decides whether this is a 90-site refactor or a one-site fail-loud** — the cheap loud answer may be strictly better.  [owns: src/functions.zig, src/charfns.zig, +datefns.zig]  [needs-oracle]
- [TODO] NOTE-invalidnumcommaspace (@pi-nd flagged, LOW cosmetic, deliberately NOT acted on — **good call, it would have diverged three sites from their unowned siblings**) — real SAS writes `'X' ,` (SPACE before the comma; Language Reference: Concepts Ch.4 Example Code 4.1, functions-ref `at line 770 column 9.`), whereas the house text everywhere — including io.zig's real-position site — is `'{s}',`. If this is ever aligned it must be done across ALL sites in one slice, never piecemeal.  [owns: various]
- [TODO] FEAT-odsoutput — `ods output <table>=<ds>;` capture of PROC output to a dataset — rejected. LARGE (needs an ODS table-capture layer); low priority for v1 clinical. Defer behind the above. main.zig + proc.zig.  [owns: src/main.zig]
- [TODO] GAP-batch-qa107 (qa round-6/7, feature-shaped fail-loud GAPs — LOW, backlog) — 6 unsupported-but-should-work surfaces, each currently fail-loud (not silent): PUT named-output of array ref (`put a{1}=`), array explicit/negative bounds (`{1:5}`,`{-2:2}`), hash find_next(). Promote individually when a real program needs one.  [owns: various]
- [BLOCKED] PERF-innerloop-charleak (perf tick-90, superlinear — but largely SUBSUMED by EPIC-charfixedwidth) — a within-row `DO` loop producing CHARACTER temporaries accumulates arena memory linearly in trip count (huge ~640B/iter const), and goes QUADRATIC (time+RSS) when the string grows. Measured ReleaseFast `/usr/bin/time -v`: fixed `$8` temp `do i=1 to N; x=put(i,8.); end;` → N=1e6 1.28s/640MB, N=1e7 22.4s/6.18GB (RSS ∝ trips for what should be O(1)); growing-cats doubling trips → ×3.5 RSS ×4 wall (quadratic); numeric-only control FLAT 1.97MB (leak is string-specific). Root cause: exec.zig scratch arena (BUG-datastepoom fix) resets ONLY at the row boundary (~L618), so a DO loop inside one runProgram piles up every put/cats/substr/upcase string result until row end. Target: within-row fixed-width char reuse = O(1) mem. **The architectural fix IS EPIC-charfixedwidth (fixed-width owned char cells → in-place assign, drop per-iter temps).** File it BLOCKED behind that epic; do NOT attempt a separate arena hack (would collide with the epic + risk correctness). Severity honest: real (key-building/delimited-string loops) but high within-row trip counts are less common than the row-count blowup already fixed.  [owns: src/exec.zig]  [deps: EPIC-charfixedwidth]
- [TODO] NOTE-bitfracround (doc-finder tick152, LOW needs-oracle) — fractional bitwise args are ROUNDED not truncated (toU32 uses @round): `band(1.9,3)=2`. SAS doesn't document non-integer handling; needs real-SAS check before changing.  [owns: src/functions.zig, src/numfns.zig]
- [TODO] NOTE-sqlcasemixfncarm (qa tick176 LOW-2, documented ceiling) — a genuine char/num CASE-arm mix escapes the static type oracle when an arm is a function-call/arithmetic (multi-token arms contribute "unknown" → assumed-consistent, ponytail-noted in sql.zig). Safe direction (under-detects, never false-positives on valid input); never errored pre-tick176. Tighten only if a real program mixes types via a function-call arm.  [owns: src/sql.zig]
- [TODO] BUG-divideinf (doc-finder tick183, MED silent-wrong — blocked on special-missing model) — `DIVIDE(x,0)` returns plain `.` instead of SAS's `.I`/`.M` (signed infinity) special missings; DIVIDE's whole purpose is to encode infinity sign. numVal (numfns.zig:213) collapses inf→missing. Ponytail ceiling: opensas has no `.I`/`.M` infinity model, so a correct fix needs that model first (larger than a one-liner). Defer until the special-missing-infinity model exists or a real program needs it.  [owns: src/numfns.zig]
- [TODO] NOTE-arrayparenput (doc-finder tick186, LOW) — paren-form `put a(2)=` fails loud while `{}`/`[]` subscripts work; SAS accepts `()` too in some contexts. Low.  [owns: src/exec.zig, src/parser.zig]  **[tick454: RE-PROBED BY ME AND STILL LIVE — `put a(2)=` gives `ERROR: expected ')' after PUT group variable list` at rc 1. But note the rc CLASS is suspect: the line itself says SAS accepts `()` in some contexts, which would make this a documented construct we do not implement, i.e. a GAP at rc 2 under D-009, not a user error at 1. Whoever takes it must settle that from the PUT entry before fixing the parse — the same wrong-class shape @pi-eb's audit found in four other rows.]**
- [TODO] FEAT-datamodify-output-point (after 42f0826, MED — blocked on storage model) — MODIFY OUTPUT (append-preserving-original) + point=/key= direct access remain FAIL-LOUD: opensas commits a MODIFY by re-emitting the whole master (io.putInput), which can't express OUTPUT's append-without-touching-others or a positional in-place write-back without preserving all untouched obs. A clean implementation needs an in-place/random-access dataset storage model (not the rebuild-commit). Defer until that exists. Cites the 42f0826 landing.  [owns: src/exec.zig, src/io.zig]
- [TODO] FEAT-procreport-2-rest5 (after _cN_ bab7ea4, LOW niche tail) — remaining fail-loud PROC REPORT COMPUTE forms: IF/THEN + multi-stmt compute, `.stat` aggregate refs in compute (`b.sum`/`sales.mean`), CALL DEFINE, character computed columns, ACROSS crossing. Increasingly niche; promote only if a real program needs one. REPORT COMPUTE core + _cN_ + before/after break + LINE all done. Cites bab7ea4.  [owns: src/proc.zig]
- [TODO] EPIC-charfixedwidth [BLOCKED: needs compile-time char-width model — do NOT one-shot] — phase1 (pi-cw) attempt REVERTED: naive width-on-first-assign (vr.len = first runtime value length) TRUNCATES computed char vars (SQL CASE 'small'→'sma', fn results 'banana'→'banan', 'CARDIAC DISORDERS'→'DISOR') = data loss; broke 7 io unit tests; export-trim (proc.zig) not done. ROOT: SAS fixes char length at COMPILE time (LENGTH stmt / inferred expression width), not first runtime value — pad-on-store needs that width source first. Triage's "bounded" estimate missed this. Re-scope before retry. [owns: src/pdv.zig, src/exec.zig, src/io.zig, src/proc.zig]
- [NOTE] GAP-divideods (tick216) = DUPLICATE of existing BUG-divideinf (blocked on .I/.M special-missing-infinity model) — do not dispatch separately; tick216 notes the NaN-payload encoding could represent .I/.M, revisit with divideinf
- [TODO] NOTE-hashpredefinedone [owns: src/exec.zig] — LOW: hash add/find before defineDone() silently accepted (SAS errors); masks a coding mistake, fail-loud-gap (tick222 F2)
- [TODO] NOTE-hashkeytypemismatch [owns: src/exec.zig] — LOW needs-confirm: char key arg vs numeric key silently returns not-found (160038) where SAS raises a type-mismatch error (tick222 F3)
- [TODO] GAP-weekufn [owns: src/functions.zig] — LOW future: standalone WEEKU()/WEEKW() numeric week-number FUNCTIONS fail loud (GAP-weekuw landed only the INTCK/INTNX intervals per scope); promote if a program needs the functions (qa tick226 NOTE)
- [TODO] GAP-callvlabel-execmacro [owns: src/functions.zig, src/exec.zig] — 2 LOW fail-loud-gaps: CALL VLABEL unsupported (twin of CALL VNAME); CALL EXECUTE of macro-statement text ('%put') not macro-processed (tick230)
- [TODO] GAP-symputrescan [DEFER: subtle — interacts with the just-landed %%SUPERQ/%%NRSTR masking; a plain runtime value rescans [owns:/% on reference but masked sentinels must NOT; needs careful interaction analysis] [owns: src/macro.zig, src/exec.zig] [after] — MED: runtime-created macro values (CALL SYMPUT / SQL INTO) never rescanned for % triggers (tick234 F4)
- [TODO] NOTE-appendcharwidth [owns: src/proc.zig] — MED (charfixedwidth-adjacent, DEFER): PROC APPEND doesn't truncate an over-length char value to BASE= width (systemic char-width-not-tracked; revisit with EPIC-charfixedwidth) (tick237 F4)
- [TODO] NOTE-stillambiguous-oracle — 4 items STILL need live-SAS (not in-repo docs): NWKDOM out-of-domain (opensas wraps, likely-should-be-missing), WEEK invalid descriptor, bitwise fractional-arg coercion, PROC EXPORT format-application (jira "likely correct" is likely WRONG — EXPORT emits put var; which honors formats → DATE9 should export 01JAN2020 not 21916; needs live-SAS check before any fix) (tick239)
- [TODO] GAP-kfunctions [owns: src/charfns.zig] — LOW fail-loud-acceptable: entire K-function family (KCOMPRESS/KUPCASE/KREVERSE/KVERIFY/...) unimplemented → fail loud (OK); implement if a real program needs one (tick243 F4)
- [TODO] GAP-transpose-oracle-tick281 [owns: src/proc.zig] — MED needs-oracle: F4 (tick281 F4)
- [TODO] GAP-arraylow-tick278 [owns: src/exec.zig, src/parser.zig] — LOW: _TEMPORARY_ leaks into PUT _ALL_ with synthetic names (F4); single subscript on a multidim array rejected (F5); DO OVER on {*} _numeric_/_character_ rejected (F6); + F7 bundle needs-oracle (tick278)
- [TODO] GAP-qa273-low [owns: various] — LOW: TITLE between DATALINES-terminator and next step dropped when no run; (F4); SORT by-unknown on a 0-VARIABLE dataset passes (F5, schemaless-empty tolerance edge); spurious uninit NOTEs for 'if 0 then set d;' hash boilerplate (F6). (F2 percent/ddmmyy was a stale-baseline false alarm — landed at corpus 1464) (tick273)
- [TODO] GAP-attrprop-low-tick272 [owns: various] — LOW batch: table43width(F6 known), uninitphantomcol(F7 ponytail), lengthbounds(F8 oracle), label256(F9 oracle) (tick272)
- [PARK] GAP-noproctitle-tick269-F6 [owns: src/proc.zig] — LOW: ODS NOPROCTITLE no-op; the auto proc-title is emitted at 10+ scattered sites — needs a shared ODS-state guard. Epic-ish, deferred by claude-gs (tick269 F6)
- [TODO] GAP-specialmiss-lowbatch [owns: src/main.zig, src/proc.zig] — LOW: MISSING statement (F3) + PROC FORMAT special-missing VALUE mapping (F4) fail-loud gaps (tick270 F3/F4)
- [TODO] BUG-wherestmtnosource **[needs-oracle flag DROPPED tick292 — doc-finder F12 found it IS doc-settled at Language Reference: Concepts p.237]** — MED silent-noop: a WHERE statement in a DATA step with no SET/MERGE (i.e. datalines INPUT) is silently ignored. Was parked as needing a live-SAS oracle; p.237 settles it, so it is now dispatchable. **One item off the ~30-deep oracle-blocked pile.**  [owns: src/exec.zig]
- [TODO] NOTE-namefmtjustify [owns: src/format.zig] [needs-oracle] — LOW: MONNAME./DOWNAME. width-less padding is currently value-LEFT (trailing blanks); WORDDATE-family is right-justified (tick250 byte evidence) so MONNAME/DOWNAME likely need right-justify too — one-line renderChar→justRight swap + update gfmt_worddate/fmt_date_defwidth goldens. Confirm vs live SAS (fmtdefwidth-date landed left)
- [TODO] NOTE-updatedupmaster [owns: src/exec.zig] — LOW needs-oracle: UPDATE with dup master BY keys omits SAS 'MASTER contains >1 obs for a BY group' WARNING (data correct) (tick258 F2)
- [PARK] NOTE-labelnowrap [owns: src/main.zig, src/proc.zig] [EPIC: default header-wrapping at blanks shifts every PRINT/REPORT golden with a label wider than its column — needs a dedicated staged effort + mass golden audit, like charfixedwidth] — MED: a label wider than its column widens the column instead of wrapping the header at blanks (SAS wraps by default without SPLIT=); both PRINT (main.zig headerWidth/printHeader) and REPORT (proc.zig emitReportLine) (tick257 F2)
- [TODO] GAP-arraymisc-tick241 [owns: src/parser.zig, src/exec.zig] — LOW batch (F4-F10): array-name/scalar collision accepted (F4); zero-element array accepted (F5); double-DOW two-SET merge unsupported/fail-loud (F6); dim/hbound non-constant dim index fail-loud (F7); no-dimension ARRAY form fail-loud (F8); first./last. of non-BY var missing+no NOTE (F9); put 1+2 drops +2 (F10). Promote individually if a real program needs one.
- [PARKED DOC-SILENT — needs an EIGHTH volume, tick371] NOTE-freqallmissnone — PROC FREQ is NOT a Base SAS procedure and has no chapter in the Procedures Guide, which explicitly defers to **'Base SAS Procedures Guide: Statistical Procedures'** — a volume we do not own. All 32 'PROC FREQ' hits are cross-references. **If more oracle work is wanted, that volume is the next one to fetch**.  [owns: src/proc.zig]
- [TODO] NOTE-putcolrewind [owns: src/exec.zig] — LOW-MED silent-wrong: backward `@n` in PUT (put 'ABC..' @1 'XY') appends instead of overwriting; append-only put buffer, documented ponytail shortcut exec.zig:4284 (tick248 F2)
